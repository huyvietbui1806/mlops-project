apiVersion: v1
kind: Service
metadata:
  name: fraud-api-service
  labels:
    app: fraud-api

spec:
  selector:
    app: fraud-api

  type: NodePort

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30800